apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: telecom-observability
data:
  SPRING_KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"
  SPRING_PROFILES_ACTIVE: "prod"
  SERVER_PORT: "8080"
